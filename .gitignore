# Binaries and build output
/bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
/trainer

# Test binaries and coverage
*.test
*.out
coverage.*

# Go workspace
go.work
go.work.sum

# Dependency directory (if vendored)
/vendor/

# Editor and OS files
.DS_Store
*.swp
.idea/
.vscode/

# Agent tooling
.pi/

# Scratch area for manual npx-skills install experiments
.tmp/

# Local skill-store fixtures placed at the repo root to test detection via
# `make run` (a real .agents / .claude store and its project lock).
.agents/
.claude/
skills-lock.json
